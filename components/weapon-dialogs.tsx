"use client";

import { useEffect, useMemo, useRef, useState } from "react";
import { useRouter } from "next/navigation";
import { ArrowLeft, ArrowRight, Check, Search, ShieldCheck, X } from "lucide-react";
import { PinConfirmDialog } from "@/components/pin-confirm-dialog";
import { InlineAlert } from "@/components/ui-system";
import { WeaponGridCard, WeaponThumbnail, type WeaponVisual } from "@/components/weapon-ui";
import { planAmmoAllocation } from "@/lib/ammunition-allocation";

type Book = { id: string; type: string; series: string; name: string };
type AmmoIssue = { id: string; registryRef: string; quantityIssued: number; ammunitionType: string };
type Issue = { id: string; registryRef: string; recipientName: string; ammoIssues: AmmoIssue[] };
type Weapon = WeaponVisual & { brand: string; serialNumber: string; productionYear: number | null; weaponSeries: string | null; accessories: string | null; notes: string | null; caliberId: string; book: Book; issues: Issue[] };
type Stock = { book: Book; available: number };

async function api<T>(url: string, init?: RequestInit): Promise<T> { const response = await fetch(url, init); const result = await response.json(); if (!response.ok) throw new Error(result.error ?? "Nie udało się wykonać operacji."); return result; }

function Steps({ active }: { active: number }) { return <div className="stepper" aria-label={`Krok ${active} z 4`}>{["Wybór broni", "Odbiorca", "Amunicja", "Podsumowanie"].map((label, index) => <div className={`step ${active === index + 1 ? "active" : active > index + 1 ? "done" : ""}`} key={label}><span>{active > index + 1 ? <Check aria-hidden /> : index + 1}</span>{label}</div>)}</div>; }

function Details({ weapon }: { weapon: Weapon | null }) {
  if (!weapon) return <InlineAlert tone="info">Wybierz egzemplarz, aby zobaczyć jego szczegóły.</InlineAlert>;
  return <><h2 className="dialog-weapon-ref">{weapon.registryRef}</h2><WeaponThumbnail weapon={weapon} sizes="300px" /><h3>{weapon.displayName || weapon.name}</h3><dl className="detail-list"><Row label="Kaliber" value={weapon.caliber.canonicalName} /><Row label="Numer seryjny" value={weapon.serialNumber} /><Row label="Rok produkcji" value={weapon.productionYear ?? "—"} /><Row label="Księga" value={`${weapon.book.series} — ${weapon.book.name}`} /><Row label="Status" value={weapon.status === "IN_STORAGE" ? "W magazynie" : "Niedostępna"} /><Row label="Wyposażenie" value={weapon.accessories || "—"} /></dl></>;
}
function Row({ label, value }: { label: string; value: string | number }) { return <div className="detail-row"><dt>{label}</dt><dd>{value}</dd></div>; }

export function IssueWeaponDialog({ open, onClose }: { open: boolean; onClose: () => void }) {
  const router = useRouter(); const [weapons, setWeapons] = useState<Weapon[]>([]); const [books, setBooks] = useState<Book[]>([]); const [stocks, setStocks] = useState<Stock[]>([]); const [loading, setLoading] = useState(false); const [error, setError] = useState(""); const [success, setSuccess] = useState(""); const [pinOpen, setPinOpen] = useState(false);
  const [step, setStep] = useState(1); const [query, setQuery] = useState(""); const [selectedId, setSelectedId] = useState(""); const [recipientName, setRecipientName] = useState(""); const [recipientReference, setRecipientReference] = useState(""); const [magazineCount, setMagazineCount] = useState(0); const [skipAmmo, setSkipAmmo] = useState(true); const [sourceBookId, setSourceBookId] = useState(""); const [ammoQuantity, setAmmoQuantity] = useState(""); const [ammunitionType, setAmmunitionType] = useState("pełnopłaszczowa");
  const searchRef = useRef<HTMLInputElement>(null); const selected = weapons.find((weapon) => weapon.id === selectedId) ?? null;
  useEffect(() => { if (!open) return; void Promise.all([api<{ weapons: Weapon[] }>("/api/weapons"), api<{ books: Book[] }>("/api/books")]).then(([weaponData, bookData]) => { setWeapons(weaponData.weapons); setBooks(bookData.books); setTimeout(() => searchRef.current?.focus(), 0); }).catch((reason) => setError(reason.message)); }, [open]);
  useEffect(() => { if (step !== 3 || !selected) return; void api<{ stocks: Stock[] }>(`/api/ammunition/stock?caliberId=${selected.caliberId}`).then((data) => { setStocks(data.stocks); setSkipAmmo(!data.stocks.length); }).catch((reason) => setError(reason.message)); }, [step, selected]);
  useEffect(() => { if (!open) return; const escape = (event: KeyboardEvent) => { if (event.key === "Escape" && !pinOpen) onClose(); }; window.addEventListener("keydown", escape); return () => window.removeEventListener("keydown", escape); }, [open, onClose, pinOpen]);
  const filtered = useMemo(() => { const needle = query.toLocaleLowerCase("pl-PL"); return weapons.filter((weapon) => !needle || [weapon.registryRef, weapon.book.series, weapon.name, weapon.displayName || "", weapon.brand, weapon.caliber.canonicalName, weapon.serialNumber].some((value) => value.toLocaleLowerCase("pl-PL").includes(needle))); }, [weapons, query]);
  const groups = { HANDGUN: filtered.filter((weapon) => weapon.type === "HANDGUN"), LONG_GUN: filtered.filter((weapon) => weapon.type === "LONG_GUN") }; const issueBook = books.find((book) => book.type === "WEAPON_ISSUE"); const ammoIssueBook = books.find((book) => book.type === "AMMUNITION_ISSUE");
  async function confirm(pin: string) { if (!selected || !issueBook) throw new Error("Brak aktywnej księgi wydawania broni."); setLoading(true); setError(""); try { const result = await api<{ issue: { registryRef: string } }>("/api/operations/issue-weapon", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ weaponId: selected.id, bookId: issueBook.id, recipientName, recipientReference: recipientReference || undefined, magazineCount, pin, ammo: !skipAmmo && ammoIssueBook ? { issueBookId: ammoIssueBook.id, sourceBookId: sourceBookId || undefined, quantity: Number(ammoQuantity), ammunitionType } : undefined }) }); setSuccess(`Wydanie zapisano jako ${result.issue.registryRef}.`); setWeapons((items) => items.map((item) => item.id === selected.id ? { ...item, status: "ISSUED" } : item)); router.refresh(); } finally { setLoading(false); } }
  if (!open) return null; const totalAvailable = stocks.reduce((sum, stock) => sum + stock.available, 0); const available = stocks.find((stock) => stock.book.id === sourceBookId)?.available ?? totalAvailable; const ammoPlan = planAmmoAllocation(stocks.map((stock) => ({ bookId: stock.book.id, available: stock.available })), Number(ammoQuantity), sourceBookId || undefined); const canNext = step === 2 ? recipientName.trim().length >= 2 : step === 3 ? skipAmmo || Boolean(Number(ammoQuantity) > 0 && Number(ammoQuantity) <= available && ammoIssueBook) : true;
  return <><div className="dialog-backdrop" role="presentation"><section className="dialog issue-dialog" role="dialog" aria-modal="true" aria-labelledby="issue-title"><header className="dialog-header"><div><h1 className="dialog-title" id="issue-title">Wydanie broni</h1><span className="muted small">Wybierz egzemplarz i przeprowadź wydanie w jednej transakcji.</span></div><button className="close-button" onClick={onClose} aria-label="Zamknij"><X /></button></header><div className="dialog-body"><div className="dialog-content">{error && <InlineAlert tone="error">{error}</InlineAlert>}{success ? <div className="confirm-card"><InlineAlert tone="success">{success}</InlineAlert><p>Stan broni i wszystkie rejestry zostały odświeżone. Wydana amunicja została zablokowana do czasu rozliczenia.</p></div> : step === 1 ? <><div className="search-hero"><Search aria-hidden /><div><input ref={searchRef} value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Wyszukaj broń…" aria-label="Wyszukaj broń" /><small>Pozycja, nazwa, marka, kaliber, księga lub numer seryjny</small></div></div><WeaponGroup title="Broń krótka" weapons={groups.HANDGUN} selectedId={selectedId} onSelect={(weapon) => { setSelectedId(weapon.id); setStep(2); }} /><WeaponGroup title="Broń długa" weapons={groups.LONG_GUN} selectedId={selectedId} onSelect={(weapon) => { setSelectedId(weapon.id); setStep(2); }} /></> : step === 2 ? <div className="confirm-card"><h2>Odbiorca</h2><div className="form-stack"><div className="field"><label htmlFor="recipient">Imię i nazwisko / nazwa odbiorcy</label><input className="input" id="recipient" value={recipientName} onChange={(event) => setRecipientName(event.target.value)} autoFocus /></div><div className="field"><label htmlFor="recipientRef">Numer dokumentu lub identyfikator</label><input className="input" id="recipientRef" value={recipientReference} onChange={(event) => setRecipientReference(event.target.value)} /></div><div className="field"><label htmlFor="magazines">Liczba magazynków</label><input className="input" id="magazines" type="number" min="0" max="100" value={magazineCount} onChange={(event) => setMagazineCount(Number(event.target.value))} /></div></div></div> : step === 3 ? <div className="confirm-card"><h2>Amunicja do {selected?.caliber.canonicalName}</h2>{stocks.length ? <div className="form-stack"><label className="checkbox-field"><input type="checkbox" checked={skipAmmo} onChange={(event) => setSkipAmmo(event.target.checked)} /> Pomiń wydanie amunicji</label>{!skipAmmo && <><div className="field"><label htmlFor="stockBook">Księga źródłowa (opcjonalnie)</label><select className="select" id="stockBook" value={sourceBookId} onChange={(event) => setSourceBookId(event.target.value)}><option value="">Dobierz automatycznie — łącznie {totalAvailable.toLocaleString("pl-PL")} szt.</option>{stocks.map((stock) => <option value={stock.book.id} key={stock.book.id}>{stock.book.series} — {stock.book.name} ({stock.available.toLocaleString("pl-PL")} szt. dostępnych)</option>)}</select></div><div className="field"><label htmlFor="ammoType">Typ amunicji</label><input className="input" id="ammoType" value={ammunitionType} onChange={(event) => setAmmunitionType(event.target.value)} /></div><div className="field"><label htmlFor="ammoQuantity">Ilość</label><input className="input" id="ammoQuantity" type="number" min="1" max={available} value={ammoQuantity} onChange={(event) => setAmmoQuantity(event.target.value)} autoFocus /><small className="muted">Dostępne po blokadach: {available.toLocaleString("pl-PL")} szt.</small></div>{Number(ammoQuantity) > 0 && <InlineAlert tone={ammoPlan.complete ? "info" : "warning"}>{ammoPlan.complete ? <>Planowana blokada: {ammoPlan.allocations.map((allocation) => { const stock = stocks.find((item) => item.book.id === allocation.bookId); return `${stock?.book.series} — ${stock?.book.name}: ${allocation.quantity.toLocaleString("pl-PL")} szt.`; }).join("; ")}</> : <>Brakuje {(Number(ammoQuantity) - ammoPlan.available).toLocaleString("pl-PL")} szt.</>}</InlineAlert>}</>}</div> : <InlineAlert tone="info">Brak dostępnej amunicji w kalibrze tej broni. Możesz wydać samą broń.</InlineAlert>}</div> : <div className="confirm-card"><h2>Podsumowanie operacji</h2><dl className="detail-list"><Row label="Broń" value={`${selected?.registryRef} — ${selected?.displayName || selected?.name}`} /><Row label="Odbiorca" value={recipientName} /><Row label="Magazynki" value={magazineCount} /><Row label="Amunicja" value={skipAmmo ? "Pominięto" : `${ammoQuantity} × ${selected?.caliber.canonicalName}`} /><Row label="Źródło" value={skipAmmo ? "—" : sourceBookId ? `${stocks.find((stock) => stock.book.id === sourceBookId)?.book.series} — ${stocks.find((stock) => stock.book.id === sourceBookId)?.book.name}` : "Dobór automatyczny"} /></dl><InlineAlert tone="info">Kliknij „Zapisz wydanie”, a następnie wpisz PIN w małym oknie potwierdzenia.</InlineAlert></div>}</div><aside className="dialog-aside"><Steps active={step} /><Details weapon={selected} /></aside></div><footer className="dialog-footer"><button className="button" onClick={step === 1 ? onClose : () => setStep((value) => value - 1)}><ArrowLeft aria-hidden />{step === 1 ? "Anuluj" : "Wstecz"}</button>{success ? <button className="button primary" onClick={() => { onClose(); router.refresh(); }}>Zamknij</button> : step < 4 ? <button className="button primary" disabled={!canNext} onClick={() => { if (step === 2) { setStocks([]); setSourceBookId(""); setAmmoQuantity(""); } setStep((value) => value + 1); }}>Dalej <ArrowRight aria-hidden /></button> : <button className="button primary" disabled={loading} onClick={() => setPinOpen(true)}><ShieldCheck aria-hidden />Zapisz wydanie</button>}</footer></section></div><PinConfirmDialog open={pinOpen} title="Potwierdź wydanie broni" onClose={() => setPinOpen(false)} onConfirm={confirm} /></>;
}

function WeaponGroup({ title, weapons, selectedId, onSelect }: { title: string; weapons: Weapon[]; selectedId: string; onSelect: (weapon: Weapon) => void }) { return <section><h2 className="weapon-section-title">{title} <span className="muted">({weapons.length})</span></h2><div className="weapon-grid">{weapons.map((weapon) => <WeaponGridCard key={weapon.id} weapon={weapon} selected={weapon.id === selectedId} onSelect={() => onSelect(weapon)} />)}</div></section>; }

export function ReturnWeaponDialog({ open, operatorName, onClose }: { open: boolean; operatorName: string; onClose: () => void }) {
  const router = useRouter(); const [weapons, setWeapons] = useState<Weapon[]>([]); const [selectedId, setSelectedId] = useState(""); const [query, setQuery] = useState(""); const [returnedAmmo, setReturnedAmmo] = useState("0"); const [error, setError] = useState(""); const [success, setSuccess] = useState(""); const [pinOpen, setPinOpen] = useState(false);
  useEffect(() => { if (open) void api<{ weapons: Weapon[] }>("/api/weapons?issued=true").then((data) => setWeapons(data.weapons)).catch((reason) => setError(reason.message)); }, [open]);
  if (!open) return null; const selected = weapons.find((weapon) => weapon.id === selectedId) ?? null; const issue = selected?.issues[0]; const linkedAmmo = issue?.ammoIssues[0]; const filtered = weapons.filter((weapon) => !query || [weapon.registryRef, weapon.name, weapon.serialNumber, weapon.caliber.canonicalName].join(" ").toLowerCase().includes(query.toLowerCase()));
  async function confirm(pin: string) { if (!issue) throw new Error("Wybierz wydaną broń."); await api("/api/operations/return-weapon", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ issueId: issue.id, returnedFromName: issue.recipientName, returnedAmmoQuantity: Number(returnedAmmo), pin }) }); setSuccess(`Przyjęto zwrot broni ${selected!.registryRef}. Rozchód amunicji: ${linkedAmmo ? linkedAmmo.quantityIssued - Number(returnedAmmo) : 0} szt.`); router.refresh(); }
  return <><div className="dialog-backdrop"><section className="dialog return-dialog" role="dialog" aria-modal="true" aria-labelledby="return-title"><header className="dialog-header"><div><h1 className="dialog-title" id="return-title">Zwrot broni</h1><span className="muted small">Lista zawiera wyłącznie aktualnie wydane egzemplarze.</span></div><button className="close-button" onClick={onClose} aria-label="Zamknij"><X /></button></header><div className="dialog-body"><div className="dialog-content">{error && <InlineAlert tone="error">{error}</InlineAlert>}{success ? <div className="confirm-card"><InlineAlert tone="success">{success}</InlineAlert></div> : <><div className="search-hero"><Search aria-hidden /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Wyszukaj wydaną broń…" autoFocus /></div><div className="weapon-grid">{filtered.map((weapon) => <button type="button" className={`weapon-card issued ${selectedId === weapon.id ? "selected" : ""}`} onClick={() => { setSelectedId(weapon.id); setReturnedAmmo("0"); }} key={weapon.id}><span className="weapon-card-ref">{weapon.registryRef}</span><WeaponThumbnail weapon={weapon} /><span className="weapon-card-name">{weapon.displayName || weapon.name}</span><span className="weapon-card-caliber">{weapon.caliber.canonicalName}</span><span className="weapon-card-status red">Wydana</span></button>)}</div>{selected && <div className="confirm-card"><h2>Przyjęcie {selected.registryRef}</h2><p>Zwracający: <strong>{issue?.recipientName}</strong></p><p>Przyjmujący: <strong>{operatorName}</strong></p>{linkedAmmo && <div className="field"><label htmlFor="returnedAmmo">Niewykorzystana amunicja zwracana z {linkedAmmo.quantityIssued} szt.</label><input className="input" id="returnedAmmo" type="number" min="0" max={linkedAmmo.quantityIssued} value={returnedAmmo} onChange={(event) => setReturnedAmmo(event.target.value)} /></div>}</div>}</>}</div><aside className="dialog-aside"><Details weapon={selected} />{linkedAmmo && <InlineAlert tone="info">Z bronią wydano {linkedAmmo.quantityIssued} szt. {linkedAmmo.ammunitionType}. System obliczy rozchód automatycznie.</InlineAlert>}</aside></div><footer className="dialog-footer"><button className="button" onClick={onClose}><ArrowLeft aria-hidden />Anuluj</button>{success ? <button className="button primary" onClick={() => { onClose(); router.refresh(); }}>Zamknij</button> : <button className="button primary" onClick={() => setPinOpen(true)} disabled={!issue}><ShieldCheck aria-hidden />Przyjmij zwrot</button>}</footer></section></div><PinConfirmDialog open={pinOpen} title="Potwierdź przyjęcie broni" onClose={() => setPinOpen(false)} onConfirm={confirm} /></>;
}
